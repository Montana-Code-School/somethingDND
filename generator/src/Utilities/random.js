function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1) + min);
}

let RandomNumBlock = function(){
    this.getRandomInt(min, max) {
      return Math.floor(Math.random() * (max - min + 1) + min);
    }
    this.classNum = 
}


class RandomNumBlock {
   constructor(
     this.generateClass = this.generateClass.bind(this)
     this.getRandomInt = this.getRandomInt.bind(this)
   )
   this.classNum = this.getRandomInt(1,9);

   getRandomInt(min, max) {
     return Math.floor(Math.random() * (max - min + 1) + min);
   }
}

const curChar = new RandomNumBlock()


curChar.classNum;
curChar.getClass()

switch(randomRace){
  case 1:
    randomSubRace = RandomNum.getRandomInt(1,2)
    if(randomSubRace === 1){
    }else{
      randomSubRace = 4
    }
    break;

   case 2:
     randomSubRace = RandomNum.getRandomInt(1,3)
     if(randomSubRace === 1){
       randomSubRace = 5
     }else if(randomSubRace === 2){

     }else{
       randomSubRace = 6;
     }
     break;

     case 3:
     randomSubRace = 3;
}

module.exports = {
  getRandomInt: getRandomInt
};
